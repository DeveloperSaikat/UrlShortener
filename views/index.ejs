<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" 
    integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
    <title>Document</title>
</head>
<body>
    <div class="container mt-5">
        <h1>URL Shortener</h1>
        <form action="/shortUrl" method="POST" class="my-4 form-inline">
            <label for="originalUrl" class="visually-hidden">Paste your Url:</label>
            <input placeholder="Enter you complete URL" type="url" name="originalUrl" id="originalUrl" class="form-control mr-2" required>
            <button type="submit" class="btn btn-primary mt-2">Shorten</button>
        </form>

        <table class="table table-striped table-responsive">
            <thead>
                <tr>
                    <th>Original URL</th>
                    <th>Short URL</th>
                </tr>
            </thead>
            <tbody>
                <% allUrls.forEach(url => { %>
                    <tr>
                        <td><a href="<%= url.original %>"><%= url.original %></a></td>
                        <td><a href="<%= url.short %>"><%= url.short %></a></td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
    </div>
</body>
</html>